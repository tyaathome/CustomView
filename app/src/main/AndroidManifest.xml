<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tyaathome.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomView">
        <activity android:name=".ScaleLayoutActivity"></activity>
        <activity android:name=".ImageFlipActivity" />
        <activity android:name=".MultiTouchViewActivity" />
        <activity android:name=".MyTextViewActivity" />
        <activity android:name=".MyNestedViewActivity" />
        <activity android:name=".MyViewPagerActivity" />
        <activity android:name=".ScableableImageActivity" />
        <activity
            android:name=".TagLayoutActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>